<%= form_with(model: song, local: true) do |form| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :song_name %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :song_author %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.text_field :gender, id: :song_gender %>
  </div>

  <div class="field">
    <%= form.label :album %>
    <%= form.text_field :album, id: :song_album %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :song_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
